<%= form_for(@program) do |f| %>
<%= render "shared/error_messages", target: @program %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
